import 'package:flutter/material.dart';
import 'package:travel_safe/models/GetDriver.dart';

class Driver extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: GetDriver("CG102021"),
    );
  }
}
